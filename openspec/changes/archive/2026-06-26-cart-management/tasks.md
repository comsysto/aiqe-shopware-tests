# Tasks: cart-management

- [x] 1.1 Create `CartPage.java` page object with `lineItems`, `quantityInput(index)`, `removeButton(index)`, and `proceedToCheckout` selectors
- [x] 1.2 Create `CartManagementTest.java` with `@BeforeAll` setup, cookie banner dismissal helper, and three test methods: `add_product_appears_in_cart`, `update_quantity_reflects_in_cart`, `remove_product_empties_cart`
